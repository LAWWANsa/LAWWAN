'use client'
import { useState } from 'react'
import { Star } from 'lucide-react'
import { submitReview } from '@/app/auth/actions'
export function ReviewForm({bookingId}:{bookingId:string}){const [open,setOpen]=useState(false);const [rating,setRating]=useState(5);const [comment,setComment]=useState('');const [busy,setBusy]=useState(false);const [done,setDone]=useState(false);const [error,setError]=useState('');if(done)return <span className="inline-flex items-center gap-2 rounded-xl bg-secondary px-4 py-2.5 text-sm font-bold"><Star className="size-4 fill-current"/> تم إرسال التقييم</span>;if(!open)return <button onClick={()=>setOpen(true)} className="rounded-xl border border-border px-4 py-2.5 text-sm font-bold">قيّم الجلسة</button>;return <div className="w-full rounded-2xl bg-secondary/60 p-4"><div className="text-sm font-extrabold">كيف كانت تجربتك؟</div><div className="mt-3 flex gap-1">{[1,2,3,4,5].map(n=><button key={n} type="button" onClick={()=>setRating(n)} aria-label={`${n} نجوم`}><Star className={`size-6 ${n<=rating?'fill-current text-primary':'text-muted-foreground'}`}/></button>)}</div><textarea value={comment} onChange={e=>setComment(e.target.value)} placeholder="تعليق اختياري" className="mt-3 min-h-20 w-full rounded-xl border border-border bg-card p-3 text-sm outline-none"/><div className="mt-3 flex gap-2"><button disabled={busy} onClick={async()=>{setBusy(true);setError('');const r=await submitReview({bookingId,rating,comment});setBusy(false);if(r.status==='success')setDone(true);else setError(r.message)}} className="rounded-xl bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground">{busy?'جارٍ الإرسال...':'إرسال التقييم'}</button><button onClick={()=>setOpen(false)} className="rounded-xl border border-border px-4 py-2.5 text-sm font-bold">إلغاء</button></div>{error&&<p className="mt-2 text-sm text-red-600">{error}</p>}</div>}
