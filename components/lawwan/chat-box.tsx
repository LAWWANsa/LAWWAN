'use client'
import { useState, useTransition } from 'react'
import { sendChatMessage } from '@/app/auth/actions'
export function ChatBox({ bookingId,currentUserId,initialMessages }:{bookingId:string;currentUserId:string;initialMessages:Array<{id:string;sender_id:string;message:string;created_at:string}>}) {
 const [messages,setMessages]=useState(initialMessages); const [text,setText]=useState(''); const [busy,start]=useTransition();
 function send(){ if(!text.trim()) return; const body=text.trim(); setText(''); start(async()=>{const r=await sendChatMessage({bookingId,body}); if(r.status==='success') setMessages(m=>[...m,{id:r.id,sender_id:currentUserId,message:body,created_at:new Date().toISOString()}]); else setText(body)}) }
 return <section className="mt-6 overflow-hidden rounded-2xl border border-border bg-card"><div className="max-h-[55vh] min-h-80 space-y-3 overflow-y-auto p-4">{messages.length===0?<div className="py-20 text-center text-sm text-muted-foreground">ابدأ المحادثة مع الطرف الآخر.</div>:messages.map(m=><div key={m.id} className={`flex ${m.sender_id===currentUserId?'justify-start':'justify-end'}`}><div className={`max-w-[82%] rounded-2xl px-4 py-3 text-sm leading-6 ${m.sender_id===currentUserId?'bg-primary text-primary-foreground':'bg-secondary text-foreground'}`}>{m.message}<div className="mt-1 text-[10px] opacity-70">{new Intl.DateTimeFormat('ar-SA',{hour:'numeric',minute:'2-digit'}).format(new Date(m.created_at))}</div></div></div>)}</div><div className="flex gap-2 border-t border-border p-3"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')send()}} className="min-w-0 flex-1 rounded-xl border border-border bg-background px-4 py-3 text-sm outline-none focus:ring-2 focus:ring-primary" placeholder="اكتب رسالتك..."/><button disabled={busy||!text.trim()} onClick={send} className="rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground disabled:opacity-50">إرسال</button></div></section>
}
