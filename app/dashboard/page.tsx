import Link from 'next/link'
import { CalendarDays, ChevronLeft, Clock3, Heart, Search, Sparkles, UserRound, Video } from 'lucide-react'
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { SiteHeader } from '@/components/lawwan/header'
import { upcomingSessions, trainers } from '@/lib/lawwan-data'

export default async function DashboardPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/auth/login')
  const name = user.user_metadata?.full_name?.split(' ')[0] || 'بك'
  return <div className="min-h-svh bg-secondary/25"><SiteHeader userName={name} /><main className="mx-auto max-w-7xl px-5 py-9 lg:px-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm font-bold text-primary">لوحة التحكم</p><h1 className="mt-1 text-3xl font-extrabold">هلا {name} 👋</h1><p className="mt-2 text-muted-foreground">وش مهارة ودك تطورها اليوم؟</p></div><Link href="/trainers" className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground"><Search className="size-4" /> اكتشف مدرب</Link></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><Stat icon={<CalendarDays />} label="الجلسات القادمة" value="2" /><Stat icon={<Heart />} label="المدربون المفضلون" value="3" /><Stat icon={<Sparkles />} label="نسبة اكتمال الحساب" value="60%" /></div><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_340px]"><section className="rounded-2xl border border-border bg-card p-6"><div className="flex items-center justify-between"><div><h2 className="text-xl font-extrabold">جلساتك القادمة</h2><p className="mt-1 text-sm text-muted-foreground">لا تفوّت جلستك القادمة</p></div><Link href="/trainers" className="text-sm font-bold text-primary">حجز جديد</Link></div><div className="mt-5 space-y-3">{upcomingSessions.map((s, i) => <div key={i} className="flex flex-col gap-4 rounded-xl border border-border p-4 sm:flex-row sm:items-center"><div className="flex size-12 items-center justify-center rounded-xl bg-primary/10 text-primary"><CalendarDays className="size-5" /></div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><h3 className="font-extrabold">{s.subject}</h3><span className="rounded-full bg-primary/10 px-2 py-1 text-[10px] font-bold text-primary">مؤكد</span></div><p className="mt-1 text-sm text-muted-foreground">مع {s.trainer}</p></div><div className="text-sm sm:text-left"><div className="font-bold">{s.date}</div><div className="mt-1 text-xs text-muted-foreground"><Clock3 className="mr-1 inline size-3" />{s.time} · {s.mode}</div></div><button className="rounded-lg border border-border px-3 py-2 text-xs font-bold"><Video className="mr-1 inline size-3.5" />الدخول</button></div>)}</div></section><aside className="rounded-2xl border border-border bg-card p-6"><h2 className="text-xl font-extrabold">أكمل حسابك</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">أكمل بياناتك لتسهيل الحجز والتواصل مع المدربين.</p><div className="mt-5 h-2 overflow-hidden rounded-full bg-secondary"><div className="h-full w-3/5 rounded-full bg-primary" /></div><div className="mt-2 text-left text-xs font-bold text-muted-foreground">60%</div><Link href="#" className="mt-5 flex items-center justify-between rounded-xl bg-secondary p-3 text-sm font-bold"><span><UserRound className="mr-2 inline size-4" />تحديث الملف</span><ChevronLeft className="size-4" /></Link><div className="mt-5 border-t border-border pt-5"><h3 className="font-extrabold">قد يعجبك</h3>{trainers.slice(0,2).map(t => <Link href={`/trainers/${t.id}`} key={t.id} className="mt-3 flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-primary text-xs font-extrabold text-primary-foreground">{t.initials}</div><div className="min-w-0"><div className="text-sm font-bold">{t.name}</div><div className="truncate text-xs text-muted-foreground">{t.specialty}</div></div></Link>)}</div></aside></div></main></div>
}
function Stat({ icon, label, value }: { icon: React.ReactNode; label: string; value: string }) { return <div className="rounded-2xl border border-border bg-card p-5"><div className="flex size-10 items-center justify-center rounded-xl bg-primary/10 text-primary">{icon}</div><div className="mt-4 text-2xl font-extrabold">{value}</div><div className="mt-1 text-sm text-muted-foreground">{label}</div></div> }
